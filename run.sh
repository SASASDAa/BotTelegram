#!/bin/bash
# Скрипт для запуска бота на Ubuntu/Linux

# Переходим в директорию, где находится этот скрипт.
# Это гарантирует, что бот будет запущен из правильной папки,
# даже если скрипт вызывается из другого места.
cd "$(dirname "$0")"

# Активируем виртуальное окружение, если вы его используете (рекомендуется).
# source venv/bin/activate

echo "Starting NameBot..."
python3 main.py